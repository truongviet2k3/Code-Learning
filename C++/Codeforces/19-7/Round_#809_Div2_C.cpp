#include <bits/stdc++.h>
using namespace std;
#define MAX 1e5+1
int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    int t, n, i;
    vector<int> arr[MAX];
    cin >> t;
    while (t--) {
        cin >> n;
        for(i = 1; i <= n; i++) {
            cin >> arr[i];
        }
        
    }
    return 0;
}